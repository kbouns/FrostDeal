{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>AvatarFilename</th>
                <td>{{ utilisateur.avatarFilename }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.Nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ utilisateur.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
            <tr>
                <th>Motdepasse</th>
                <td>{{ utilisateur.Motdepasse }}</td>
            </tr>
            <tr>
                <th>DateInscription</th>
                <td>{{ utilisateur.DateInscription ? utilisateur.DateInscription|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_register_index') }}">back to list</a>

    <a href="{{ path('app_register_edit', {'id': utilisateur.id}) }}">edit</a>

    {{ include('register/_delete_form.html.twig') }}
{% endblock %}
